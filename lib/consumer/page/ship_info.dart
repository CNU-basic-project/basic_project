import 'package:flutter/material.dart';

class ConsumerShipInfo extends StatelessWidget {
  const ConsumerShipInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
